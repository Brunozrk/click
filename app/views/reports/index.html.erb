<section class="content-header">
  <h1>Reporte de horas</h1>
</section>
<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box">
        <div class="box-header">
          <h3 class="box-title">Listagem <%= sign(current_user.total_balance) %></h3>
          <%= render 'search_form' %>
          <%= render 'print_menu' %>
        </div>
        <%= render 'list' %>
      </div>
    </div>
  </div>
</section>
